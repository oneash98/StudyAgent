Generate a concise set of seed terms for the requested Keeper concept-set domain.

Requirements:
- prefer clinically specific terms
- include common synonyms when useful
- avoid vague placeholders
- do not include explanatory prose
- return only JSON matching the schema
