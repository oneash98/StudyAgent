Filter candidate OMOP concepts for the requested Keeper concept-set domain.

Requirements:
- keep only concepts that are clinically relevant to the target phenotype or differential-diagnosis context
- do not include narrative explanation outside the JSON response
- return only JSON matching the schema
